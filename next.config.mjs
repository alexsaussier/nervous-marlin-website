/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['nervousmarlin.com', 'image.daisyui.com', 'asaussier-projects.s3.amazonaws.com']
    

  },
};

export default nextConfig;
